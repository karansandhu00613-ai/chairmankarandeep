logs/
node_modules/

# Runtime state written by the services. Never commit: these hold account
# records and session tokens, and on Render the disk is wiped anyway.
karan-data.json
karan-sessions.json
data.json
sessions.json
dashboard-users.json
dashboard-sessions.json
# Chairman OS runtime state. OWNER_CREDENTIALS.txt holds a plaintext bootstrap
# password and owner.json holds the credential hash; neither belongs in a repo.
OWNER_CREDENTIALS.txt
owner.json
chairman-data/
